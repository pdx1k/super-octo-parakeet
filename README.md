# super-octo-parakeet

3D life simulation — see pull request for full implementation.

## Lichen

An incremental game, mobile-first. Source: `public/lichen/index.html` — a single
self-contained page (no build step, no dependencies). Vite copies `public/` into
`dist/`, so it deploys to GitHub Pages at `/lichen/`.
